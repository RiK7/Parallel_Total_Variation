Requirement:OpenCV (,CUDA if you want to run CUDA version)

    There is our code and input videos. To compile our code, you just need to cd to the selection version and run the build.sh file. And for running file, just run the run.sh file.
    If you want to know more parameter you can input, just append "-h" at the end of run.sh.
